import 'package:flutter/material.dart';

class BudgetOverviewPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Budget Overview'),
      ),
      body: Center(
        child: Text('Budget Overview Page'),
      ),
    );
  }
}
